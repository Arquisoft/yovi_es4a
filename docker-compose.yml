services:

  mongodb:
    image: mongo:latest
    container_name: mongodb
    # Mongo no debería estar abierto a internet, por lo que no exponemos el puerto fuera del contenedor
    ports:
      - "27017:27017"
    networks:
      - monitor-net
    volumes:
      - mongo-data:/data/db
  gateway:
    build: ./gateway
    container_name: gateway
    image: ghcr.io/arquisoft/yovi_es4a-gateway:latest

    ports:
      - "80:80"    # Tráfico HTTP (Local/Redirección)
      - "443:443"  # Tráfico HTTPS (Azure)
    volumes:
      - ./gateway/certs:/etc/nginx/certs:ro # Para los certificados en Azure
      - ./gateway/letsencrypt:/etc/letsencrypt #Guarda certificados permanentemente

    depends_on:
      - users
      - webapp
      - gamey
    networks:
      - monitor-net

  users:
    build: ./users
    container_name: users
    image: ghcr.io/arquisoft/yovi_es4a-users:latest
    # ports: - "3000:3000" ELIMINADO: Ya no se accede directamente
    depends_on:
      - mongodb
    environment:
      - NODE_ENV=development
      - MONGODB_URI=mongodb://mongodb:27017/yovi
      - EMAIL_USER=${EMAIL_USER}  # variables para el envío del correo
      - EMAIL_PASS=${EMAIL_PASS}
      - FRONTEND_URL=${FRONTEND_URL} # redirecciona a esta URL tras verificar cuenta
    networks:
      - monitor-net

  webapp:
    build:
      context: ./webapp
      args:
        # La API ahora se accede a través de la ruta relativa del Gateway
        - VITE_API_URL=/api/users 
    container_name: webapp
    image: ghcr.io/arquisoft/yovi_es4a-webapp:latest
    # ports: - "80:80" <-- ELIMINADO: Nginx servirá la web
    depends_on:
      - users
    networks:
      - monitor-net

  gamey:
    build: ./gamey
    container_name: gamey
    image: ghcr.io/arquisoft/yovi_es4a-gamey:latest
    # ports: - "4000:4000" <-- ELIMINADO
    networks:
      - monitor-net
      
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./users/monitoring/prometheus:/etc/prometheus
    networks:
      - monitor-net

  grafana:
    image: grafana/grafana
    container_name: grafana
    ports:
      - "9091:3000"
    volumes:
      - ./users/monitoring/grafana/provisioning:/etc/grafana/provisioning
    networks:
      - monitor-net

networks:
  monitor-net:
    driver: bridge

volumes:
  mongo-data:
